{
  "name": "comic-docket",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "compile:sass": "sass src/scss/styles.scss src/style.css -w",
    "deploy": "firebase deploy"
  },
  "devDependencies": {
    "sass": "^1.94.1",
    "vite": "^7.1.7"
  },
  "dependencies": {
    "firebase": "^12.5.0"
  }
}
